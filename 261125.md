# Daily Report - mercredi 26 Nov. 2025

### to-do list
[x] update TIL rep\
[] SQL module 3 first half done\
[x] visit Christmas market at Tuleries garden !

### what I learned today
1. LIKE, '%', '_'
- LIKE
- '%' : 0-multiple characters (LIKE 'ap%' > u may search 'apple', 'ape', 'apply', etc at once)
- '_' : one single character (LIKE 'c_p' > u may search 'cap', 'cop', etc)

2. using multiple tables
~~~
SELECT D.DEP_NAME , E.F_NAME, E.L_NAME
FROM EMPLOYEES as E, DEPARTMENTS as D
WHERE E.DEP_ID = D.DEPT_ID_DEP
ORDER BY D.DEP_NAME, E.L_NAME DESC;
~~~



