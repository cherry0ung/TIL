# Daily Report - samedi 29 Nov. 2025

### to-do list
- [ ] module 4
- [x] module 3 review 
- [ ] 

à partird du module 3 de SQL, je laisse de coté les "Hands-on Labs" qui sont optionnels, \
je reviendrai une fois que j'aurai terminé tous les cours.


### what I learned today
1. FROM_DAYS()
   convert days into date form

2. SQL APIs

| 💾 Appli ou DB | 🛠️ SQL API  | 
| :---:   | :---: |
| MySQL   | MySQL C API |
| PostgreSQL | psycopg2   | 
| IBM DB2   | ibm_db |
| SQL Server | dblib API   | 
| DB access for MS WindosOS   | ODBC | 
| Oracle | OCI   | 
| Java | JDBC   | 

3. Connection methods
   - .cursor()
   - .commit()
   - .rollback()
   - .close()
   - .callproc()
   - .execute()
   - .executemany()
   - .fetchone()
   - .fetchmany()
   - .fetchall()
   - .nextset()
   - .arraysize()

4. basic codes using DB-API
```python 

from dbmodule import connect

# Create connection object
Connection = connect('databasename', 'username', 'pswd')

# Create a cursor object
Cursor = connection.cursor()

# Run Queries
Cursor.excute('select' * from 'mytable')
Results = cursor.fetchall()

# Free resources - don't forget
Cursor.close()

```

